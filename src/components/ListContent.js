import React from "react";

class ListContent extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return <tbody id="book-list"></tbody>;
  }
}

export default ListContent;
